@page "/addpatient"
@rendermode InteractiveServer

@using HospitalManagementSystem.Data
@using HospitalManagementSystem.Models
@using Microsoft.AspNetCore.Identity
@using Microsoft.AspNetCore.Authorization
@using Microsoft.AspNetCore.Components.Authorization
@using MudBlazor

@inject AuthenticationStateProvider AuthenticationStateProvider
@inject UserManager<ApplicationUser> userManager
@inject NavigationManager navigation
@inject ISnackbar Snackbar

@attribute [Authorize]

<div class="mx-2">
    <MudPaper Elevation="15" Class="p-4">

        <EditForm Model="patient" OnValidSubmit="CreateNewPatient" FormName="addpatient">

            <DataAnnotationsValidator />

            <h3>Add New Patient</h3>

            <MudGrid>

                <MudItem xs="12" sm="6" md="4">
                    <label>First Name <span class="text-danger">*</span></label>
                    <MudTextField @bind-Value="patient.FirstName"
                                  For="@(() => patient.FirstName)"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                    <ValidationMessage For="@(() => patient.FirstName)" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Middle Name</label>
                    <MudTextField @bind-Value="patient.MiddleName"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Last Name <span class="text-danger">*</span></label>
                    <MudTextField @bind-Value="patient.LastName"
                                  For="@(() => patient.LastName)"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                    <ValidationMessage For="@(() => patient.LastName)" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Phone Number</label>
                    <MudTextField @bind-Value="patient.PhoneNumber"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>ID Number</label>
                    <MudTextField @bind-Value="patient.IdNumber"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Email Address</label>
                    <MudTextField @bind-Value="patient.EmailAddress"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Date Of Birth</label>
                    <MudTextField @bind-Value="dateOfBirth"
                                  InputType="InputType.Date"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Gender</label>
                    <MudSelect @bind-Value="selectedGenderId"
                               Variant="Variant.Outlined"
                               FullWidth="@true"
                               T="int?"
                               Placeholder="Select gender">
                        @foreach (var g in Genders)
                        {
                            <MudSelectItem Value="@((int?)g.Id)">@g.Description</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Blood Group</label>
                    <MudSelect @bind-Value="selectedBloodGroupId"
                               Variant="Variant.Outlined"
                               FullWidth="@true"
                               T="int?"
                               Placeholder="Select blood group">
                        @foreach (var b in BloodGroups)
                        {
                            <MudSelectItem Value="@((int?)b.Id)">@b.Description</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>

                <MudItem xs="12" sm="6" md="4">
                    <label>Marital Status</label>
                    <MudSelect @bind-Value="patient.MaritalStatusId"
                               Variant="Variant.Outlined"
                               FullWidth="@true"
                               T="int?"
                               Placeholder="Select marital status">
                        @foreach (var m in MAS)
                        {
                            <MudSelectItem Value="@((int?)m.Id)">@m.Description</MudSelectItem>
                        }
                    </MudSelect>
                </MudItem>

                <MudItem xs="12" sm="6" md="6">
                    <label>Guardian Name</label>
                    <MudTextField @bind-Value="patient.GuardianName"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="6">
                    <label>Guardian Phone</label>
                    <MudTextField @bind-Value="patient.GuardianPhoneNumber"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" md="12">
                    <label>Address</label>
                    <MudTextField @bind-Value="patient.Address"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="6">
                    <label>Remarks</label>
                    <MudTextField @bind-Value="patient.Remarks"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

                <MudItem xs="12" sm="6" md="6">
                    <label>Allergies</label>
                    <MudTextField @bind-Value="patient.Allergies"
                                  Variant="Variant.Outlined"
                                  FullWidth="@true" />
                </MudItem>

            </MudGrid>

            <div class="mt-4">
                <MudStack Row Justify="Justify.SpaceBetween">

                    <MudButton Href="/patients"
                               Variant="Variant.Filled"
                               Color="Color.Warning"
                               StartIcon="@Icons.Material.Filled.ArrowBack">
                        Back To List
                    </MudButton>

                    <MudButton ButtonType="ButtonType.Submit"
                               Variant="Variant.Filled"
                               Color="Color.Primary"
                               StartIcon="@Icons.Material.Filled.Save">
                        Save Patient
                    </MudButton>

                </MudStack>
            </div>

        </EditForm>

    </MudPaper>
</div>

@code {

    public Patient patient { get; set; } = new();
    private DateTime? dateOfBirth;
    private int? selectedGenderId;
    private int? selectedBloodGroupId;

    public List<SystemCodeDetail> AllSystemCodeDetails = new();
    public List<SystemCodeDetail> Genders = new();
    public List<SystemCodeDetail> BloodGroups = new();
    public List<SystemCodeDetail> MAS = new();

    public string? UserId;

    protected override async Task OnInitializedAsync()
    {
        try
        {
            var authState = await AuthenticationStateProvider.GetAuthenticationStateAsync();
            var user = authState.User;

            if (user.Identity?.IsAuthenticated == true)
            {
                var currentUser = await userManager.GetUserAsync(user);
                UserId = currentUser?.Id;
            }

            AllSystemCodeDetails = await systemCodeDetailsService.GetSystemCodeDetailsAsync() ?? new();

            Genders = AllSystemCodeDetails
                .Where(d => d.SystemCode?.Code?.ToLower().Contains("gender") == true)
                .ToList();

            BloodGroups = AllSystemCodeDetails
                .Where(d => d.SystemCode?.Code?.ToLower().Contains("blood") == true)
                .ToList();

            MAS = AllSystemCodeDetails
                .Where(d => d.SystemCode?.Code?.Contains("MAS", StringComparison.OrdinalIgnoreCase) == true)
                .ToList();
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Load error: {ex.Message}", Severity.Error);
        }
    }

    protected async Task CreateNewPatient()
    {
        try
        {
            if (dateOfBirth.HasValue)
                patient.DateOfBirth = dateOfBirth.Value;
            if (selectedGenderId.HasValue)
                patient.GenderId = selectedGenderId.Value;
            if (selectedBloodGroupId.HasValue)
                patient.BloodGroupId = selectedBloodGroupId.Value;

            patient.CreatedOn = DateTime.UtcNow;
            patient.CreatedById = UserId;
            patient.ModifiedOn = DateTime.UtcNow;
            patient.ModifiedById = UserId;

            if (string.IsNullOrWhiteSpace(patient.PatientNo))
                patient.PatientNo = $"PT-{DateTime.UtcNow:yyyyMMddHHmmss}";

            await patientService.AddPatientAsync(patient);

            Snackbar.Add("Patient created successfully", Severity.Success);

            navigation.NavigateTo("/patients?created=true");
        }
        catch (Exception ex)
        {
            Snackbar.Add($"Error: {ex.Message}", Severity.Error);
        }
    }

    public void BackToList()
    {
        navigation.NavigateTo("/patients");
    }
}
